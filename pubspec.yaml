name: flutter_web_app
description: Flutter web app for Jenkins-Docker-Ngrok integration

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
